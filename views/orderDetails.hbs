    <header>
        <button>
            <a href="/" id="homeButton">Home</a>
        </button>
        <h1>Manage Order Details</h1>
    </header>

    <main>
        <form id="addOrderDetailsForm" method="POST" action="/add-orderDetail-form">
            <label for="orderID">Order ID:</label>
            <input type="text" id="orderID" name="orderID" placeholder="Enter order ID" required>

            <label for="bookID">Book ID:</label>
            <input type="text" id="bookID" name="bookID" placeholder="Enter book ID" required>

            <button type="submit">Add Order Details</button>
        </form>

        <table id="orderDetailsTable">
            <thead>
                <tr>
                    <th>Order ID</th>
                    <th>Book ID</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <!-- Sample Order Details rows -->
                {{#each orderDetails}}
                    {{> orderDetail}}
                {{/each}}
            </tbody>
        </table>

	<h2>Updating Order Details</h2>
        <p>To update a specific order's details, select the order ID from the drop-down menu and enter the new book ID</p>
        <form id="update-order-details-form-ajax">

            <label for="input-order-id">Order ID: </label>
            <select name="input-order-id" id="orderIDSelect">
                <option value="">Select an Order ID</option>
                {{#each orderDetails}}
                <option value="{{this.orderID}}">{{this.orderID}}</option>
                {{/each}}
            </select>

            <label for="input-book-id">Book ID: </label>
            <input type="text" id="input-book-id" name="input-book-id" placeholder="Enter new Book ID" required>

            <input type="submit" value="Update Order Details">
        </form>
    </main>
</body>

